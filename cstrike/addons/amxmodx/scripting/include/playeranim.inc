#if defined _playeranim_included
#endinput
#endif
#define _playeranim_included

#pragma library "playeranim"

native playeranim_start(user, anim, gait, Float:framerate, end_frame, loop)
native playeranim_reset(user)